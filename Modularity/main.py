import math_utils

# Use the circle_area() function from the math_utils module
radius = 5
area = math_utils.circle_area(radius)
print("The area of a circle with radius", radius, "is", area)

# Use the circle_circumference() function from the math_utils module
circumference = math_utils.circle_circumference(radius)

print("The circumference of a circle with a radius", radius, "is", circumference)

# Use the factorial() function from the math_utils module
n = 6
fact = math_utils.factorial(n)
print("The factorial of", n, "is", fact)